{% extends "base.njk" %}

{% block title %}Jugadores | Nuevo{% endblock %}

{% block contenido %}
<style>
  body {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    min-height: 100vh;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  }
  
  .form-container {
    max-width: 600px;
    margin: 40px auto;
    padding: 0 20px;
  }
  
  .form-card {
    background: white;
    border-radius: 15px;
    box-shadow: 0 8px 16px rgba(0,0,0,0.2);
    overflow: hidden;
  }
  
  .form-header {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 30px;
    text-align: center;
  }
  
  .form-header h1 {
    margin: 0;
    font-size: 2rem;
    font-weight: bold;
  }
  
  .form-body {
    padding: 40px;
  }
  
  .form-group {
    margin-bottom: 25px;
  }
  
  .form-group label {
    font-weight: 600;
    color: #495057;
    margin-bottom: 8px;
    display: block;
  }
  
  .form-control, .form-select {
    border-radius: 8px;
    border: 2px solid #e9ecef;
    padding: 12px 15px;
    transition: border-color 0.3s;
    width: 100%;
  }
  
  .form-control:focus, .form-select:focus {
    border-color: #667eea;
    box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
    outline: none;
  }
  
  .btn-primary {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border: none;
    border-radius: 8px;
    padding: 12px;
    font-weight: 600;
    width: 100%;
    transition: transform 0.2s, box-shadow 0.2s;
    color: white;
    cursor: pointer;
  }
  
  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 8px rgba(102, 126, 234, 0.3);
    background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);
  }
  
  .alert {
    border-radius: 8px;
    margin-bottom: 20px;
    padding: 12px 15px;
  }
  
  .alert-danger {
    background-color: #fee;
    border: 1px solid #fcc;
    color: #c33;
  }
  
  .text-danger {
    color: #dc3545;
    font-size: 0.875rem;
    margin-top: 5px;
  }
  
  .btn-secondary {
    background: #6c757d;
    border: none;
    border-radius: 8px;
    padding: 12px;
    font-weight: 600;
    width: 100%;
    margin-top: 10px;
    transition: transform 0.2s, box-shadow 0.2s;
    color: white;
    text-decoration: none;
    display: inline-block;
    text-align: center;
  }
  
  .btn-secondary:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 8px rgba(0,0,0,0.2);
    background: #5a6268;
    color: white;
    text-decoration: none;
  }
</style>

<div class="form-container">
  <div class="form-card">
    <div class="form-header">
      <h1>🏀 Nuevo Jugador</h1>
    </div>
    
    <div class="form-body">
      {% if errores.general %}
        <div class="alert alert-danger">{{ errores.general }}</div>
      {% endif %}

      <form action="/players" method="post">
        <div class="form-group">
          <label for="nickname">Nickname:</label>
          {% if errores.nickname %}
            <p class="text-danger">{{ errores.nickname }}</p>
          {% endif %}
          <input type="text" class="form-control" name="nickname" id="nickname"
            placeholder="Ej: sllull" value="{{ datos.nickname }}" required>
        </div>

        <div class="form-group">
          <label for="name">Nombre completo:</label>
          {% if errores.name %}
            <p class="text-danger">{{ errores.name }}</p>
          {% endif %}
          <input type="text" class="form-control" name="name" id="name"
            placeholder="Ej: Sergio Llull" value="{{ datos.name }}" required>
        </div>

        <div class="form-group">
          <label for="country">País:</label>
          {% if errores.country %}
            <p class="text-danger">{{ errores.country }}</p>
          {% endif %}
          <input type="text" class="form-control" name="country" id="country"
            placeholder="Código del país (Ej: ES)" value="{{ datos.country }}" maxlength="2" required>
        </div>

        <div class="form-group">
          <label for="birthDate">Fecha de nacimiento:</label>
          {% if errores.birthDate %}
            <p class="text-danger">{{ errores.birthDate }}</p>
          {% endif %}
          <input type="date" class="form-control" name="birthDate" id="birthDate"
            value="{{ datos.birthDate }}" required>
        </div>

        <div class="form-group">
          <label for="role">Posición:</label>
          {% if errores.role %}
            <p class="text-danger">{{ errores.role }}</p>
          {% endif %}
          <select class="form-select" name="role" id="role" required>
            <option value="">Selecciona una posición...</option>
            <option value="base" {% if datos.role == "base" %}selected{% endif %}>Base</option>
            <option value="escolta" {% if datos.role == "escolta" %}selected{% endif %}>Escolta</option>
            <option value="alero" {% if datos.role == "alero" %}selected{% endif %}>Alero</option>
            <option value="ala-pivot" {% if datos.role == "ala-pivot" %}selected{% endif %}>Ala-Pívot</option>
            <option value="pivot" {% if datos.role == "pivot" %}selected{% endif %}>Pívot</option>
          </select>
        </div>

        <button type="submit" class="btn btn-primary">Crear Jugador</button>
        <a href="/players" class="btn-secondary">Cancelar</a>
      </form>
    </div>
  </div>
</div>
{% endblock %}